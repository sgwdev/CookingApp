
<div class="dynamic-list-element ingredient">
    <%= f.label :name, "Ingredient : " %>
    <%= f.collection_select :ingredient_id, Ingredient.all, :id, :name, {:prompt => true} %>
    <%= f.label :amount, "Amount :" %>
    <%= f.number_field :amount %>
    <%= f.collection_select :unit_id, Unit.all, :id, :symbol, {:prompt => true} %>
    <%= f.hidden_field :_destroy if @recipe.persisted? %>
</div>


